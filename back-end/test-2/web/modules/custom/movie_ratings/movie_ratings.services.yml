services:
  movie_ratings.rating_manager:
    class: Drupal\movie_ratings\RatingManager
    arguments:
      - '@entity_type.manager'
      - '@entity_field.manager'
      - '@request_stack'
      - '@current_user'
